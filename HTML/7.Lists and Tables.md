Lists in HTML

HTML provides two main types of lists:

1. Ordered List (ol)

Displays items in numbered format (1, 2, 3…)

Used when order matters (steps, rankings, instructions)

Each item is written using the li tag

👉 Example idea: recipe steps, instructions, procedures

2. Unordered List (ul)

Displays items with bullet points

Used when order does NOT matter

Items are also written using the li tag

👉 Example idea: features list, shopping list, menu items


Basic Table Structure

Tables are used to display data in rows and columns.

Main tags used in tables:

table → creates the table

tr (table row) → defines a row

th (table header) → header cell (bold + centered by default)

td (table data) → normal data cell

👉 Structure flow:

table
 └── tr (row)
      ├── th (header) / td (data)
      ├── th / td

👉 In short:

Lists → organize items

Tables → organize data in rows & columns